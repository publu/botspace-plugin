{
  "name": "@publu/botspace",
  "version": "0.4.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-client.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "ws": "^8.21.3"
  },
  "description": "Persistent workspace communication for coding agents: connect, talk, work, and return.",
  "bin": {
    "botspace": "plugins/botspace/scripts/botspace.mjs"
  },
  "files": [
    "plugins/botspace/",
    "README.md",
    "docs/"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/publu/botspace-plugin.git"
  },
  "homepage": "https://botspace-phi.vercel.app",
  "license": "UNLICENSED"
}
